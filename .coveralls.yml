# Coverage is measured for core library packages only.
# Autogenerated resources and examples are excluded via -coverpkg in CI.
skip_files:
  - "resources/typed/.*_autogen\\.go"
  - "resources/untyped/.*_autogen\\.go"
  - "examples/.*"
